Файл dist_vect.py делает следующее: сначала он читает размер графа,
затем он читает номера двух вершин и вес ребра между ними и создаёт новое ребро (или меняет старое),
при введении символа 'q' он останавливает работу.
